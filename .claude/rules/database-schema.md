# 데이터베이스 스키마

> 테이블별 컬럼 상세 스키마는 `.claude/references/database-schema.md` 참조.

- DB: PostgreSQL 16 / localhost:5432 / testdb / postgres:postgres
- 테이블: `junior` (JuniorTask), `senior` (SeniorReview), `lead` (LeadDecision)
- API: GET/POST `/role-data/{role}` (role: junior, senior, lead)
